# Photo Gallery Website

A simple Flask-based photo gallery web application.

## Features
- View photos in a responsive gallery grid
- Click photos to see full details
- Mobile-friendly design
- Navigation menu (Home, Gallery, About)

## Installation
1. Install Flask: `pip install flask`
2. Run the app: `python app.py`
3. Open browser to `http://localhost:5000`

## Project Structure
- `app.py` - Main Flask application
- `templates/` - HTML templates
- `static/` - CSS and images

## Requirements
- Python 3.x
- Flask

## Future Enhancements
- Dynamic photo data from database
- File upload feature
- User authentication
- Search functionality